let frutas = ['🍎', '🍌', '🍓']

//Frutas es el array con este bucle lo que hacemos en guardar
//en la variable fruta el elemento del array correpondiente en cada vuelta
for (let fruta of frutas) {
  console.log(fruta); // imprime el elemento
}